# Contributing

1. Fork the repo
2. Create a branch: `git checkout -b feature/my-change`
3. Make changes
4. Run `shellcheck bin/review-cycle` locally
5. Submit a PR against `main`

Please keep PRs focused — one feature or fix per PR.
