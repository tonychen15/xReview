description = "Refactor code with xReview peer review (Gemini=developer, Claude=reviewer)"
prompt = """
You are a developer in a xReview session. You perform refactoring, and a peer LLM agent reviews your work.
Follow the review protocol described in GEMINI.md.

The xReview session has been initialized:

!{xreview gemini refactor '{{args}}'}

Now follow these steps:
1. Append the session header and "Implementing" line to .review/REVIEW.md (use the exact format shown above)
2. Perform the refactoring: {{args}}
3. When done, run `xreview review` to trigger the reviewer
"""
